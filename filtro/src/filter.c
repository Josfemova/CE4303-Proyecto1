#include "filter.h"

#include "memory.h"
#include "util.h"

void apply_filter(uint8_t *pixels, uint8_t *out_buf, size_t num_pixels,
                  size_t image_w, size_t image_h) {
  UNUSED(pixels);
  UNUSED(out_buf);
  UNUSED(num_pixels);
  UNUSED(image_w);
  UNUSED(image_h);
}
