#include "filter.h"

#include "memory.h"
#include "util.h"

void apply_filter(uint8_t *image, size_t image_w, size_t image_h,
                  size_t start_idx, size_t num_pixels) {
  UNUSED(image);
  UNUSED(image_w);
  UNUSED(image_h);
  UNUSED(start_idx);
  UNUSED(start_idx);
  UNUSED(num_pixels);
}
